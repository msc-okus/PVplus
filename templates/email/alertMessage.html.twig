{% extends 'email/baseEmail.html.twig' %}

{% block content %}
    <row class="welcome">
        <columns>
            <spacer size="35"></spacer>
            <h1>{{ email.subject }}</h1>
            <spacer size="10"></spacer>
        </columns>
    </row>
    <spacer size="30"></spacer>
    <row>
        <columns>
            <p>{{ message|raw }}</p>
        </columns>
    </row>
{% endblock %}