{% extends 'base.html.twig' %}

{% block title %}
    {{  parent() }}special operations
{% endblock %}

{% block styles %}
    {{  parent() }}
    {{ encore_entry_link_tags('special_export') }}
    <link href="{{ absolute_url(asset('css/monthly_report_inline.css')) }}" rel="stylesheet" type="text/css">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('special_export') }}
    {#
    <script src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script>
        $(function () {
            let daysInMonth = $('#createLifeRepoting_Monthly_daysinmonth').val();
            $('#createLifeRepoting_Monthly_startday').on('change', function (e) {
                let optionSelected = $("option:selected", this);
                let startday = parseInt(this.value);
                let restDays = daysInMonth - startday;


                $('#createLifeRepoting_Monthly_endday').empty();
                $('#createLifeRepoting_Monthly_endday').append($('<option>', {
                    value: 0,
                    text : 'please choose',
                    selected: true
                }));
                let i = startday;
                while (i <= daysInMonth) {
                    $('#createLifeRepoting_Monthly_endday').append($('<option>', {
                        value: i,
                        text : i
                    }));
                    i++;
                }
            });
        });
    </script>
    #}
{% endblock %}

{% block body %}
    <div class="grid-x">
        <div class="cell">
            <h1>{{ headline }}</h1>
        </div>
    </div>

    {% include'live_reporting/_inc/_createFormByDate.html.twig' %}
    <hr>
    {% if report != null %}
        <div class="grid-x">
            <div id="special_export_buttons"></div>
            <div class="cell">
                {{ include('report/_monthly/_tableSub.html.twig', {excel: 'true'}) }}
            </div>
        </div>
        {% if tickets is defined %}
            {% if tickets|length > 0 %}
                <div class="grid-x">
                    <div class="cell">
                        {% include('report/_base_layouts/_ticketsSub.html.twig') %}
                    </div>
                </div>
            {% endif %}
        {% endif %}
    {% endif %}
{% endblock %}
