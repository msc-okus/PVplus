
{% if (invArray != null) %}
    <div class="grid-x">
        <div class="cell small-form" >
            <h3>Inverter List</h3>
            <input  id="search-check-all" type="checkbox"
                    data-ticket-list-target="switch"
                    data-action="click->ticket-list#selectAll">

            <label for="search-check-all" class="">Select All</label>
        </div>
    </div>
    <hr>
    <div class="grid-x small-up-3 medium-up-6 large-up-9 xlarge-up-12">
        {% for key in trafoArray|keys %}
            {% set trafo = trafoArray[key] %}
            <div class="cell small-form">
                <input id="search-trafo{{ loop.index }}"
                       class="js-checkbox-trafo"
                       data-ticket-list-first-param="{{ trafo.first }}"
                       data-ticket-list-last-param="{{ trafo.last }}"
                       data-ticket-list-trafo-param="{{ loop.index }}"
                       data-action="click->ticket-list#checkTrafo" type="checkbox">
                <label for="search-trafo{{ loop.index }}">TS {{ key }}</label>
            </div>
        {% endfor %}
    </div>
    <hr>
    <div class="grid-x small-up-2 medium-up-4 large-up-6 xlarge-up-9">
        {% for inverter in invArray %}
            <div class="cell small-form">
                <input id="search-I-{{ loop.index }}" type="checkbox"
                       class="js-checkbox"
                       name="{{ inverter['inv'] }}"
                       data-action="click->ticket-list#setInverter">
                <label for="search-I-{{ loop.index }}"
                       class="">{{ inverter['inv'] }}</label>
            </div>
        {% endfor %}
    </div>
{% else %}
    No Inverters found to select
{% endif %}