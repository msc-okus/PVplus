
{% block body %}
    {% set mt = messagetext %}

    <div class="dropdown-content" {{ stimulus_controller('processmessenges->getshowmessage()', { message: mt})}}>
    </div>

{% endblock %}
{% block styles %}
    <link href="{{ absolute_url(asset('build/app.css')) }}" rel="stylesheet" type="text/css">
{% endblock %}
