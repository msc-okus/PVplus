{% extends 'base.html.twig' %}

{% block body %}
    <div class="grid-x grid-margin-x grid-padding-x">
        <div class="cell">
            <h1>Edit User {% if user.locked %}<span style="color: red; text-transform: uppercase;">this User is locked</span>{% endif %}</h1>
        </div>
    </div>
    {{ include('user/_form.html.twig') }}
{% endblock %}


{% block title %}
    {{  parent() }}edit User
{% endblock %}


{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('user_edit') }}
{% endblock %}