{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}Tools
{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}

{% block body %}
    <div class="grid-x">
        <div class="cell">
            <h1 id="thechange">ImportTools</h1>
        </div>
    </div>

    {{ form_start(importToolsForm, {'attr': {'novalidate': 'no'}}) }}

    <div class="grid-x grid-margin-x">
        <div class="cell medium-2">
            {{ form_row(importToolsForm.anlage) }}
        </div>
        <div class="cell medium-2">
            {{ form_row(importToolsForm.startDate) }}
        </div>
        <div class="cell medium-2">
            {{ form_row(importToolsForm.endDate) }}
        </div>
        <div class="cell medium-2">
            {{ form_row(importToolsForm.importType) }}
        </div>
        <div class="cell medium-2">
            {{ form_row(importToolsForm.function) }}
        </div>
    </div>

    <div class="grid-x grid-margin-x" style="min-height: 50vh;">
        <div class="cell medium-12">
            <div class="callout" >
                {{ output|raw }}
            </div>
            <div class="" style="max-height: 50vh; overflow-y: scroll;"
                    {{ stimulus_controller('tools-message', {
                        url: path('app_log_messages_list'),
                        refreshIntervale: 5000,
                    }) }}>
            </div>
        </div>
    </div>

    <div class="grid-x grid-margin-x grid-margin-y">
        <div class="cell medium-4">
            {{ form_widget(importToolsForm.calc) }}
            {{ form_widget(importToolsForm.close) }}
        </div>
    </div>
    {{ form_end(importToolsForm) }}
{% endblock %}